
Server:
  port: 9000

spring :
  datasource:
    url: jdbc:postgresql://localhost:5432/testHateoas
    username: postgres
    password: 391738
    driver-class-name: org.postgresql.Driver
  jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
          show_sql: true
          format_sql: true